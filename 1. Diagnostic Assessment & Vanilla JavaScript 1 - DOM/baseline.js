// `$('#todos')`
document.getElementById("todos");

// `.text()`
element.textContent;

// `.text("hello!")`
element.innerHTML = "hello!";

// `.val()`
element.children[0];

// `.prepend("hello")`
document.getElementById("example").innerHTML = "hello" + document.getElementById("example").innerHTML;


// `.addClass('bold')`
element.classList.add('bold');

// $(document).ready()
document.addEventListener("DOMContentLoaded", function(event) {
    
});
